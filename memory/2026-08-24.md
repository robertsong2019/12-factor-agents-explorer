# 2026-08-24

## 00:00~03:15 key-development-1 (C506v) — side-channel A/B PRE 臂 + full-500 官方刷新

**Cycle 定位**: C506 (embedding side-channel, d05780b/e046989) 的 A/B 验证——tsv 明示"验证待跑"。本轮跑了 PRE 臂全量 + POST 臂（踩踏中仍在飞）。

**成果 (commit e0abf3a)**:
- **full-500 官方刷新 @ C506 HEAD: exact 0.368**（C499 ref 0.316，+0.052；hit 0.396 / evhit 0.910 / abstain 8.6%）——C499 后首个全量参考
- 分类明细 vs C499: **multi_session 0.128→0.233 (+0.105)**（C503 enum_count + C505 duration-family 全量兑现，超切片预期）/ **ssu 0.329→0.457 (+0.129)**（C501 role-answer 兑现，当时"在飞"担忧正式解除）/ kupdate +0.026 / temporal 0.602→0.609 / ssa 0.286 / pref 0.000 平（side-channel 目标类，待 POST）/ **零类目回归**
- 套件验证: 3187 passed + `test_large_flush_performance` 负载假败（C465 已记录，隔离复测 2.02s 过 + 集成文件 16/16）；本轮零代码改动

**POST 臂 (--sidechannel) 状态**: pending。全量跑被 OOM 边缘踩踏拖垮（CPU 效率 0.7%），改 126q 切片（pref30+ssa56+ctl40，ijson 流式提取 65MB slice.json），启动后仍被挤至停滞——**当前仍在飞 (/tmp/c507/post_slice.json, pid 1781179)**，预期 ssa evhit 0.929→~1.0 / pref hit 0.567→~0.87。完成后对比脚本就绪: /tmp/c507/compare.py + pre_full500.json (per-question 可直接对齐)。

**本轮环境教训（重要）**:
1. **三大 dev 会话并行之夜**: /tmp/c507 有另一会话做 "two-entity total-number" 取证（counting 线）、/tmp/c508 有 where-question 原型线——1.9GB 盒同时跑三个 eval/原型 = 全员换页踩踏（D 态、CPU 效率 0.7~30% 波动）。**教训: 大 JSON/嵌入 eval 前先 `ps` 查有无其他 python 会话在飞；有则改切片或改期**
2. `free -m | head -2` 截掉了 Swap 行——此盒有 10GB swap 且已用 4.2GB，"无 swap"是误判；fallocate /swapfile 打到现役 swap 文件幸好失败（Text file busy 自保护）
3. 判定字段: judge=exact 模式下 per-row `correct_exact=None`，权威=`correct`（top-level `accuracy_exact`）——聚合脚本读错字段得出 exact 0.000 假读数
4. ijson 流式提取 277MB→65MB 切片方案成立（峰值内存 ~10MB 级），pip 装 ijson 需 --break-system-packages

**experiments.tsv**: 项目仓 +1 行 keep（e0abf3a）。趋势: overall exact 0.316→**0.368**。

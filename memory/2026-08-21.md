# 2026-08-21

## 02:00 知识整理 (knowledge-organization-morning)

**整合范围**: 过去 24h（08-20 02:00 → 08-21 02:00）— **C484-C489 六连（时序答案侧机制族主体）** + Research #077/#078 同日研究→生产 + 工具线 skill-test/mcpt 双复活 + 博客 1 篇 + AI×Neuro #17。

**本轮最有价值的发现（非流水账）**:
1. **temporal-133 五连零翻转**: 0.271 (C481 ref) → 0.323 (C482) → 0.376 (C486) → 0.444 (C488) → **0.474 (C489)**，36→63/133。全部 A/B 双臂验证、全部 +N/−0。零 LLM 路径上单调爬升的第三周。
2. **C489 取证钥匙 = 精度截断静默排序 bug**: haystack_dates 自带 minute 粒度，生产 parse_lme_date 截断 date-only → 同日 session 并列退化为 session index 排序。继 C470 uuid 排序、dict 插入序之后第三种"静默决定顺序"的来源。`_session_dates_raw` stash 修复。
3. **弃权也是答案（第三次命中）**: C486 负存在弃权（Google _abs）+ C489 一锚+零提及→abstain（两个 _abs GT 全判对）。sub-24h-tie fallthrough 墙保护 7 对（含 3 对已对）——同日推荐回声相隔几小时，强行排序是伪精度。诚实分层：>24h 可比 / <24h 不可比 / 零提及→弃权。
4. **gate placement 是正确性面（第二次）**: C489 pairwise 须放 temporal_arith **之前**——TA first-kind 也 claim which-first 家族且错解 2 题；5 个 TA-correct 经 pairwise 同答验证。继 C482 "counting d 错 fire 8 题" 后再证：两个形式族重叠时，对的那族必须先 claim。
5. **mcpt CLI 自诞生 100% 不可用**: init.js L98 引号失配 = node --check 即炸 + validate/generate/test/serve 四模块 import 但从未存在。4 bug 一次修齐 0→38。工具"存在"≠工具"能跑"——hermetic 测试是唯一真相源。

**验证**: pytest collect 实测 **9768** ✓（9647→9682→9694→9733→9768）；git 311eeed/82ce1ff/b9815ee/80cdc8c 在库；项目仓 experiments.tsv 链完整（C484/C485 标 needs_verification/pending——无独立 A/B，但 C486 pre-arm 0.323 精确复现 C482 reference → 基线未扰动，非回滚）。

**MEMORY.md 更新**（10 edits）: Current Focus 08-20→08-21、Active Theme 前插 484-489 arc（297→298 天）、表格 amg 9647→9768 + 新 API 项、四项目 11788→11909、全项目 ~20661→~20782、最高优先级同步、arc 节改名 "475-489 时序/计数答案侧机制族六连" + C488/C489 两条 + Next 重写（full-500 rerun 上位头号）、**insight #245**（原始时间精度截断=静默排序 bug 第三源；弃权也是答案）。

**HEARTBEAT.md 更新**（9 edits）: 标题 08-21 Friday、amg 9768（链更新）、四项目 11909、全项目 ~20782、298 天（C482→C489 全 keep 连续 8 轮）、新增 08-20 晚~08-21 凌晨活动节（C486-C489 + C484/485 注记 + 工具线 + 博客/神经科学）、Next dev targets 重写（past-perfect ✅ / order ✅ 已落标，full-500 rerun 上位）、上次检查新条目。

**experiments.tsv 趋势**: workspace tsv 健康（skill-test/mcpt/blog 三条）；amg cycle 条目在项目仓 tsv（已知结构性缺口 monitoring-only）。temporal 弧线全记录：0.045→0.180→0.226→0.271→0.323→0.376→0.444→**0.474**（8 轮）。

状态: amg 9768 / 零回滚 298 天（KO 链）。**C490 候选 = full-500 rerun（逾期头号，投影 overall ~0.24+）/ 29-family 残余 8 unanchored / TA first-kind 弃用评估**。

## 02:04 知识整理·验证轮 (重复触发)

02:00 主轮已完成全量整合（commit e6c5f7c），本轮仅核实，**全部通过、无需修正**：
① pytest collect 实测 **9768** ✓ ② C486/C488/C489 commits b9815ee/82ce1ff/311eeed 在库 ✓ ③ 四项目 11909 验算通过（9768+571+1570）④ 02:01 后仅 MEMORY.md/HEARTBEAT.md/2026-08-21.md 三个知识文件变更（均系主轮自身），无新产出落盘。
同 08-15/08-16/08-20 验证轮模式：重复触发无害，主轮工作已被 git 锚定。

## 03:00 项目测试循环（autoresearch）— afm 0→29 tests, 8 bugs

目标：`tools/agent-framework-manager`（afm CLI，1993 行，零测试）。commit b667a98。

**基线**：0 tests / CLI 完全不可用（deps 未装）→ `npm install --omit=dev` 后可载入。

**8 个真 bug（全部测试证实后修复）**：
1. `await fs.readFile(p,'utf8').trim()` 优先级 — `.trim()` 作用在 Promise 上，TypeError 被 catch 吞掉 → getAgentStatus 对活进程永远报 stopped 并删 pid 文件（3 处：AgentManager + Monitor×2）。整个"运行状态"功能 DOA。
2. `afm init` 调用不存在的 `configManager.init` → CLI init 必失败。改调 agentManager.init。
3. `name:editAgents` 缺引号 → 构建 inquirer prompt 数组即 ReferenceError。
4. 三个类 constructor 不 await 异步 loadConfig → 构造后 config 为 undefined/null（竞态）。改为同步 fs。
5. startAgent 用 exec 启动 detached 进程 — exec 不返回 pid → pid 文件写 "undefined"；生成脚本 import './agent-runner.js' 相对 .afm/scripts 必 module-not-found。改 spawn+unref+绝对 file URL。
6. parseUptime 把 MM:SS 当 HH:MM:SS（05:30→19800s 而非 330s，两处）。
7. updateStats 首样本均值=Infinity + requests 每任务双计数（performAgentTask 和 updateStats 各加一次）。
8. `agent --status` 调 getAgentStatus 但从不打印结果。
附带：checkPermissions 在未 init 目录必失败；npm test 指向不存在的 jest 配置 → 改 `node run-tests.js`。

**验证**：29/29 × 3 runs。含真实 spawn→pid 记录→stopAgent kill 的 e2e。

**坑**：node --test 子进程 runner 协议又 corruption（"Unable to deserialize cloned data"）→ 沿用 8/18 方案：直接 `node test/afm.test.js` 进程内跑。exec 工具对 `node --test` 触发 preflight 拒绝 → 用 run-tests.js 包装。

experiments.tsv 已记录。

---

## Documentation Morning Cron (04:00)

**任务**: 文档完善 cron 执行

**发现**: agent-memory-graph README 漂移 — 徽章 9579 vs 实际 9768 (C489)，对比表 Tests 7400 陈旧，C484-C489 temporal QA 家族（6 cycles）零文档覆盖

**完成**:
1. **新建 `projects/agent-memory-graph/TUTORIAL-TEMPORAL-QA.md`** (~4KB) — C456-489 temporal QA 教程：
   - 核心洞察：temporal 问题不是 when 问题，是时长算术+排序
   - 三律：form gate / 不编造宁可弃权 / zero-flip
   - 路由级联：order_sort → pairwise → temporal_arith → pp_duration/pure_tenure → counting
   - 关键概念：最早-FRESH 锚定、子句粒度、负存在弃权、sub-24h tie
   - temporal-133 演进表 0.323→0.474
   - 所有导入和形态门行为已实际验证（11 imports OK，4 form gate demos pass）
2. **README 外科手术更新**：徽章 9579→9768、对比表 7400→9768、新增 temporal QA 特性条目、教程链接 ×3

**测试**: temporal 相关 121 tests passed (0.28s)

**提交**: 08e63d5

## 05:03 Cron: essay-generation-morning

**主题**: 排序从不中立：三种静默排序 bug 的取证与诚实的平局处理（C470 uuid/插入序 + C489 精度截断——昨夜 C489 取证钥匙的工程面转化，与昨日"提及卫生"内容面正交不重复）

- 本地存档: memory/daily-posts/2026-08-21-silent-ordering-honest-ties.md
- 博客: posts/silent-ordering-bugs-honest-ties-2026-08.html（~2400 字，4 代码块：粒度截断对照、sorted 稳定性退化链、C470 确定性修复、sub-24h 三层决策矩阵 + 三源对照表 + 六条守则）
- 发布: commit b638ffd，Pages 200 OK（05:06 CST 验证），index 置顶
- **过程发现**: 本地 blog 仓落后远端 3 commits（昨日 8d50e70 在远端而本地无）→ 先 pull --ff-only 再推。今后 blog 推送前先 fetch 比对。

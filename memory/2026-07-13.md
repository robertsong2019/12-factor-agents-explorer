
## 深度研究 #006 (20:00) — Context Engineering: Beyond Retrieval

**主题**: Agent Memory 的上下文工程层 — 检索质量 ≠ 生成质量

**4 篇核心论文**:
1. **Apple Shared Selective Memory** (2607.09493) — 全量历史比无记忆更差(71%<79%)，选择性4类达96%。零token数据刷新14×加速
2. **SWE-MeM** (2606.28434, CUHK+ByteDance) — 自适应压缩工具 compress(analysis,start,end,content,remaining_work) + Memory-aware GRPO. SWE-Bench 60.2%
3. **PLACEMEM** (2607.04089) — 记忆胶囊统一语义+KV cache. 级联失效. TTFT -61%. stale hits 0
4. **Is GraphRAG Needed?** (2606.25656, AWS+Cisco, ACL 2026 GEM) — 9场景. 上下文工程19-53% token减少. 检索-生成鸿沟量化

**可运行代码**: `ContextEngineeringLayer` TypeScript class — selective filter → staleness check → scoring → budget-aware select → adaptive compress → token-efficient serialize. `npx tsx` 验证通过.

**新增 MEMORY.md 洞察**: #28-31 (全量历史反模式/压缩可学习/语义计算统一/检索指标高估)

**新增行动项**:
- add() category 参数 (~40+30行)
- invalidate() 级联失效 (~60+40行)
- retrieval_quality_eval utilization_rate (~25+20行)
- context_engineering_layer (~150+80行)

笔记: `catalyst-research/exploration-notes/2026-07-13-context-engineering-beyond-retrieval.md`

## 20:13 — Deep Exploration #007: Proactive Memory & Geometric Time

**主题**: Agent Memory 的两个下一个范式转移：主动认知 + 几何时间

**3 篇核心论文**:
1. **RoMem** (2604.11544, Edinburgh+LIGHTSPEED) — 连续相位旋转 + Semantic Speed Gate → 几何阴影解决时间冲突。SOTA: ICEWS05-15 72.6 MRR, MultiTQ 2-3× MRR, LoCoMo SOTA。Append-only 架构，无需 LLM 仲裁。
2. **CogniFold** (2605.13438, OpenNorve) — 三层 CLS 扩展(event→concept→intent) + 拓扑自组织。LoCoMo 81.23%, LongMemEval 93.0%。CogEval Proactivity 0.614 (唯一非零)。“不完美即机制”哲学。
3. **SkillGraph** (May 2026, USTC) — 技能图进化：节点=技能，边=组合关系，RL 进化。

**可运行代码**: ~400行 TypeScript 实现 PhaseRotationMemory + CognitiveFoldingEngine。验证 3/3 assertions pass:
- ✅ 永久事实 (born_in) 永远满分 1.0
- ✅ Biden (0.0032) > Obama (0.0000) for president_of at NOW
- ✅ 13 cognitive nodes from 7 events, 3 intents crystallized

**新增 MEMORY.md 洞察**: #32-33 (时间=关系属性/意图拓扑涌现)

**新增行动项**:
- Semantic Speed Gate (edge volatility) ~40+30行
- crystallize_intents() API ~80+20行
- read_proactive_context() API ~60+15行
- temporal_score() 连续相位旋转 ~50+25行

笔记: `catalyst-research/exploration-notes/2026-07-13-proactive-memory-geometric-time.md`

### autoresearch 质量检查
- [x] 含可运行代码（~400行 TypeScript, 3/3 assertions pass）
- [x] 独到见解（时间是关系属性/不完美即机制/append-only几何阴影>破坏性更新/三层proactive context）
- [x] 项目关联（10项映射到 amg, 5条优先实现路径, 4个 API 提案）
- [x] 文献覆盖（10篇论文 + 3篇认知科学基础）
- [x] 可操作下一步（按 ROI 排序, 含 LOC 估算）

## 22:10 — Evening Dev: amg Cycle 236

**项目:** agent-memory-graph
**基线:** 2860 tests

### Cycle 236: invalidate_cascade() + add(category=) + search_by_category() ✅

**来源论文:**
1. **PLACEMEM** (2607.04089) — cascade invalidation: when a fact becomes invalid, propagate to all nodes that depend on it
2. **Apple Shared Selective Memory** (2607.09493) — category-based selective retrieval: filter memory by type (preference/protocol/episodic/reference/skill)

**实现:**
- `invalidate_cascade(node_id, reason, invalidated_by, cascade_relations, max_depth)` — BFS traversal over `depends_on` (reverse lookup: find sources pointing to current) + `enables` (forward lookup: find targets from current). Cycle-safe via visited set. Idempotent. 13 tests.
- `add(category=)` parameter — backward-compatible column migration. Category stored in new `category` column on nodes table.
- `search_by_category(category)` — returns nodes matching category, excludes quarantined. 7 tests.

**测试:** +20 tests (2860→2880), all pass, 0 regressions
**提交:** ca0f74d

### 总结
- 2860 → 2880 tests (+20)
- 227th consecutive day without rollback
- Research-driven: PLACEMEM cascade + Apple Selective Memory category

## 23:00 — Key Dev 1: amg Cycle 237

**项目:** agent-memory-graph
**基线:** 2880 tests

### Cycle 237: read_proactive_context() ✅

**来源论文:**
- **CogniFold** (2605.13438) — proactive context assembly: instead of reactive retrieval (waiting for query), use crystallized intent nodes to proactively push relevant memory

**实现:**
- `read_proactive_context(active_intents, top_k, min_temperature, include_intents)` — pipeline: find intent nodes → filter by active_intents (ID or label substring) → gather abstracted members → score by cache_temperature inline → deduplicate → return per-intent context bundles sorted by member count
- Members include `linked_intents` showing which intents reference each node
- Parameters: active_intents filter (by ID or label substring), top_k limiting, min_temperature threshold, include_intents metadata toggle
- Self-reference excluded, quarantined nodes excluded, top_k <= 0 returns empty

**测试:** +24 tests (2880→2904), all pass, 0 regressions, 21.5s
**提交:** 951e4ea

### 总结
- 2880 → 2904 tests (+24)
- 228th consecutive day without rollback
- Completes CogniFold proactive memory trilogy: crystallize_intents (c235) → read_proactive_context (c237)

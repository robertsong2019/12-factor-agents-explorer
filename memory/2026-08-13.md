
## 20:00 — Deep Exploration #062: GraphRAG 2026 全景与 amg 定位

**Research #062**: GraphRAG 前沿全景分析

### 完成内容
- 系统调研了 2025-2026 GraphRAG 生态：MS GraphRAG, HippoRAG2 (ICML 2025), Graph-R1 (ICML 2026), AutoGraph-R1 (ACL 2026), LinearRAG (ICLR 2026), LazyGraphRAG
- 建立了 amg vs 主流 GraphRAG 系统的能力对标矩阵（16个维度）
- 识别出 amg 的 7 项独有优势（时序、遗忘、consolidation、compression、entropy、security、multi-agent）
- 识别出 amg 的 4 项关键缺失：自动KG构建、Passage Nodes、Recognition Filter、RL优化检索
- 3个可运行代码示例：PPR检索、Lazy社区检测、Spreading Activation
- 5条关键洞察 + 3个actionable next steps

### 关键发现
1. **amg的差距是叙事差距，不是技术差距** — PPR/community/spreading activation 已覆盖甚至超越 HippoRAG2 和 MS GraphRAG，但完全没有用 "GraphRAG" 框架定位自己
2. **自动KG构建是入场券** — 没有 extract_from_text()，amg 只是图算法库而非完整GraphRAG系统
3. **GraphRAG市场爆发中** — $6.27B (2026) → $28.45B (2030)，多跳准确率 86% vs 32%

### 笔记位置
`catalyst-research/exploration-notes/2026-08-13-graphrag-2026-landscape-amg-positioning.md`

## 21:00 — Code Lab: FastAppendQueue + Knowledge Freshness (Cycles 425-427)

### 基线
- 8505 tests, 52460 lines memory_graph.py

### Cycle 425: FastAppendQueue — System-1/System-2 Dual-Process Write Path
- Research #033: Engram 83.6% vs 73.2%, every SOTA system separates hot/col.
- System-1: O(1) buffer append, keyword search, no graph ops.
- System-2: flush with deduplication, link-by-kind/tags, full graph integration.
- Auto-flush threshold (default 50). 4 consistency modes (session/causal/eventual/committed).
- drain() for external processing. status() with full diagnostics.
- +55 tests (8505 → 8560).

### Cycle 426: knowledge_freshness_report() — FAMA-Aware Graph Diagnostic
- Research #051: FAMA penalizes stale memory by 15-43 points.
- 5 time buckets (fresh/recent/aging/stale/decayed).
- Per-kind freshness breakdown (avg/min/max).
- Weighted freshness score (by node weight).
- Stalest/freshest node lists (top 10).
- Actionable recommendation text.
- +21 tests (8560 → 8581).

### Cycle 426b: FastAppendQueue Extended Operations
- flush_and_consolidate() — combines flush + NREM/REM consolidate in one call.
- pending_kinds()/pending_categories() — buffer composition.
- peak_buffer_size() — capacity tracking.
- is_healthy() — queue health check with issue detection.
- +22 tests (8581 → 8603).

### Cycle 427: peek() + E2E Integration Tests
- peek(n) — preview buffer items without removal.
- 6 E2E agent simulation tests (session lifecycle, burst pattern, cross-session dedup).
- 4 performance edge cases (1K appends <0.5s, 500-item flush <2s).
- +16 tests (8603 → 8619).

### 结果
- **8619 tests** (8505 → 8619, +114)
- **52891 lines** (52460 → 52891, +431)
- 4/4 cycles kept (100% pass rate throughout)
- **291st consecutive day** 🏆

### 更新
- MEMORY.md: 新增 GraphRAG 生态定位章节 + 5个action items

### 20:04 — Research #063: OpenClaw Plugin Architecture for amg
- **Topic**: OpenClaw 插件体系深度解析 + amg 集成三条路径 + 可运行原型
- **Method**: 分析 6 个现有 OpenClaw 插件 (local-embedding-memory, openclaw-tavily, openclaw-qqbot, wecom, adp-openclaw, ddingtalk) + PluginRuntime SDK 类型 + MCP 配置路径
- **Key findings**:
  1. MCP 协议是分发核武器 — amg 已有的 17-tool MCP server 零代码即可集成
  2. Skill 是给AI的"营销材料" — SKILL.md = prompt engineering, 不是代码
  3. 三层分发漏斗: MCP Server(基座✅) → Skill Extension(包装) → ClawHub(市场)
  4. OpenClaw memory-core 是竞品分析对象 — amg 每个维度严格升级
  5. 推荐 Path B: Skill Extension (~60行) 作为中间态
- **Deliverables**: 完整 openclaw.plugin.json + SKILL.md + package.json + Python 安装脚本
- MEMORY.md: 更新 amg OpenClaw plugin action item + 新增分发策略章节
- 笔记: `catalyst-research/exploration-notes/2026-08-13-openclaw-plugin-architecture-amg-distribution.md`

## Tool Development Evening (22:00-22:20)

**nano-agent Round 17** — 3 features shipped, 27 tests, all green.

- **F58 `search_boolean`** — Boolean AND/OR/NOT search with parenthesized grouping. Union-find recursive evaluator. Falls through to plain search when no operators present.
- **F59 `condense`** — Near-duplicate merge using union-find clustering. Combines tags (union), importance (max), timestamp (earliest), content (longest). Returns merge report.
- **F60 `export_markdown_table`** — GitHub-flavored table export with tag filter, limit, content truncation, pipe escaping, importance bars.

Total: 991 → 1018 tests (+27), zero regressions. Commit `7cbf1b1`.

## Autoresearch Evening (22:02)

**amg Cycle 428: `extract_from_text()`** — Rule-based KG construction from raw text. 1 cycle, 1 feature, 22 tests.

- **Problem**: Research #062 识别出 `extract_from_text()` 是 amg 成为完整 GraphRAG 系统的"入场券" — 没有自动KG构建，amg 只是图算法库
- **Design**: 零外部依赖的规则提取器：句子分割 (.!?;\n) → 实体检测（大写短语/引号术语）→ 关系模式匹配（7种）→ 去重 → 图构建
- **7 relation patterns**: `is_a`, `works_at`, `created`, `located_in`, `has`, `part_of`, `built`
- **Entity types**: 1-3词大写短语, 双引号术语, 单引号术语
- **Dedup**: 按 label 精确匹配现有节点，存在则复用
- **Result dict**: nodes_created, edges_created, entities[], relations[], sentences

8619 → 8641 tests (+22). 52891 → 53055 lines (+164). Commit `8f3fb41`.
291st consecutive day 🏆. Zero regressions (2462 existing tests verified).

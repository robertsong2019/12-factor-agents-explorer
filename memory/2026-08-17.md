
## 03:25 autoresearch 测试循环 (cron)
- **项目:** prompt-mgr（上次 TSV 记录 07-31@102，实际已自然增长至 267）
- **Bug 修复（真实 bug）:** `Template.render()` 把变量值直接当 re.sub replacement 字符串 → `C:\new\folder` 被静默损坏成 `C:\new\x0colder`（\f→换页符），`\1` 直接抛 invalid group reference。用 lambda 替换修复（字面量语义）。
- **F15:** `TemplateCollection.export_markdown(tags, sort_by)` — 单文档导出：生成时间戳 + TOC 锚点 + tag 过滤（all 语义）+ name/created/updated 排序（未知值回退 name）。
- **结果:** 267→283（+16 测试），283/283 全过，0 回归。commit b21d0ee。
- 基线扫描：amg-mcp 自定义脚本测试正常（Day-10 29 tests pass）、agent-log 12/12 脚本健康（昨日修过）。

## 04:00 Cron: documentation-morning

**amg README 追平 Cycle 449→457 + code-lab 进化史**（commit 4228629 / 报告 2e36e56）

- amg README：新增「压缩残差、保留遗忘与对抗鲁棒 (Cycles 449-457)」章节（11 条目，签名全部源码 grep 后撰写）；badge 9241→9406（pytest --collect-only 实测）
- code-lab 进化史 +9 行（449-457），里程碑段重写为「压缩残差+对抗鲁棒+时间推理」
- 本批亮点叙事线：C452 决定性负发现（置信度门分不开 cat-5）→ C455 subject_support_gate 闭环破解——负发现→专用闸门的完整弧线值得后续 blog 素材池记录
- 确认 knowledge-org 无重叠

## GitHub 创新点分析（cron 19:20）
- 报告存至 `memory/github-trending-analysis-2026-08-17.md`
- 🚨 头号事件：**deepseek-ai/deepseek-harness** 4天 145k stars，"Everything is a Plugin"，基于 cordis 时空可组合性框架，harness 大战升级
- 其他重点：prime-agent（自我改进 RLM）、TencentDB-Agent-Memory（团队记忆，适配 OpenClaw）、needle（14MB 端侧 tool-calling 模型）、semantica（开源 Palantir）
- 趋势：harness=新OS、团队级记忆、skills 资产化、模型体量两极分化

## 深度技术研究 (deep-exploration-evening 20:00, Research #069)
- **主题**: LLM-as-Judge for agent memory benchmarks — amg cat5/kupdate 残差语义判分立项（HEARTBEAT Next dev target）
- **来源动机**: C454 kupdate acc 0.0 但 retrieval_hit 1.0（协议病）+ C455 cat5 谓词语义唯一剩余路径
- **产出**: `catalyst-research/exploration-notes/2026-08-17-llm-judge-agent-memory-benchmarks.md` + 可运行原型 `code/llm_judge_amg.py`（6 case 全预期判定通过，mock 降级模式零依赖，--real 留 ollama 接口）
- **关键发现**: ① 参考锚定二元判定 = LongMemEval leaderboard 可比口径 ② Memora 3-judge 原子判据多数票 κ0.86-0.90；辩论式 judge 反而放大偏置 ③ κ-deflation：test-retest≠效度（一致但偏象限）④ FAMA 遗忘感知指标重排系统名次 → amg bi-temporal 原生支持 = 差异化叙事窗口 ⑤ MemEval 公平性：judge accuracy ≠ token F1（Zep 94.7% vs 复测 F1 0.35-0.50），README 基准表必须双列口径
- **竞品数字**: MemEval LoCoMo 1986q — PropMem judge 0.823 / OpenClaw 0.725 / cat5 列 Graphiti 0.873 全场最高（KG 结构性拒答优势）
- **下一步**: ① judge_llm() 落地 amg 真身仓双口径输出（~1 cycle）② ollama 安装解锁 --real 实测+双首跑① ③ FAMA for amg 研究→博客
- **工具状态**: Tavily 432 配额仍耗尽（08-14 起），AnySearch 稳定替代（本轮 2 轮 10+10 结果）
- MEMORY.md 已更新（Research #069 条目 + 近期研究一览表首行）

## 晚间深度研究 + 博客闭环 (20:13)
- Research #069 (LLM-as-Judge for Agent Memory Benchmarks) + #070 (OTel GenAI semconv) 已于 20:00/20:04 产出（笔记在 exploration-notes/）
- 博客闭环基于 #069 完成：`posts/llm-judge-reliability-agent-eval-2026-08.html`，首页卡片已插入，commit 00918aa 已 push
- #070 (OTel GenAI) 未转化博文，留作候选素材

## code-lab-evening（cron 13a2f284, 21:00-22:20）

amg 真身仓 4 cycles 全 KEEP，基线 9453 → 9485（100% pass），全部已 push：

- **C461 8a9f93c** telemetry v2 对齐 semantic-conventions-genai @c739977：动词化 span 名、`gen_ai.memory.store.id`、单一 `gen_ai.memory.record.count`、`gen_ai.memory.query.text` Opt-In 门控、非规范键迁 `amg.*`
- **C462 4002ee7** judge_llm() 双口径判分（Research #069 核心）：ollama qwen2.5:7b + mock 降级 + 多数投票 + calibration_summary(>25% divergence → RECALIBRATE)，evaluate(judge_mode="dual") 双列 accuracy
- **C463 afcb662** --judge CLI + run_eval dual 聚合，2 题 mock 冒烟全通（8月底 full-500 LME_s run 就绪）
- **C464 4b096be** LoCoMo evaluate_sample/run_locomo/CLI 三层 dual 接线，cat5 判定协议级共享

关键设计：judge auto-detect 一次性 probe ollama 失败永久降级 mock（sticky _JUDGE_MODE），流水线永不因 judge 不可用而中断；correct_llm 三态 True/False/None(ERROR)。测试坑：locomo argparse edit 曾留孤儿行致 IndentationError，py_compile 先行可早发现。

## 22:15 tool-development-evening（cron）
- nano-agent Round 20（F67-F69）commit 1af2fb5：序列协议 dunders、copy()+content_hash()（SHA-256 canonical state fingerprint）、subscribe/unsubscribe 事件钩子（add/remove/update 含 batch，observer 异常吞掉，维护类操作静默）
- 测试 1106→1156（+50），零回归
- 教训：content_hash 测试先按"独立构建同内容→同哈希"写，失败后才意识到 timestamp 属于状态——先明确指纹语义再写断言

## 22:05 tool-development-evening（cron，本轮）
- **项目选择**: 🔴 顶级项全 human-blocked → 中优先级最高未完项 = lab/agent-observability OTel GenAI 对齐（Research #070 今日 20:04 刚完成，明确 "~1 cycle"，且 amg telemetry v2 C461 已同批做完，零重叠）
- **成果 (commit 9705e4e, pushed)**: `lab/agent-observability/src/otel-genai.ts` 落地
  - mapSpan：6-op 映射（agent.run→invoke_agent {name} / llm.call→chat {model} / tool.execute→execute_tool {name} / retrieval.search→retrieval / memory.write→upsert_memory / memory.read→search_memory）
  - Opt-In 内容门控：env `OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT` + 显式 flag，默认 OFF
  - lintGenAiSpans：5 规则 CI 门禁（Required 属性/span 名格式/error.type/Opt-In 泄漏/整数 usage）
  - exportGenAiOtlp：OTLP-JSON resourceSpans（32-hex traceId/16-hex spanId/ns 单调时间）
  - 零侵入验证：真 Tracer getSpans() 直接喂适配器（integration tests）
- **测试**: 192→222 (+30)，TDD 一次全绿，零回归
- **对原型唯一改动**: OTLP traceId 从 spans[0] 全局改为 per-span（正确性微修，同 trace 无行为差异）
- **坑**: `git add lab/agent-observability` 扫入 .understand-anything/ 面板静态资产（某分析工具生成的 dashboard，14 文件随 commit 入库，无害但脏；下次提交前先检查 git status 排除或补 .gitignore）
- experiments.tsv 已记 222/222 keep

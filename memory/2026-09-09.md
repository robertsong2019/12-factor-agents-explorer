# 2026-09-09

## C559 (key-development-2, 00:00) — amg name-demand definitional-anaphora face ✅

**Baseline**: C558 (kd-1, 23:51) 289/500 (0.578), suite 10347-noted/10352-measured
**Result**: 289→290 (0.578→0.580), +1 rescue c4f10528, 0 kills — commit 66adc0a

### 循环
1. **目标**: queue 第一项 `c4f10528 (GT raw=2 cross-sentence evidence)`
2. **诊断**: impostor "Take a cooking class: …nasi goreng…" (raw=3) 赢过 GT bearer
   "Miss Bee Providore: This restaurant serves…" (raw=2) — 跨句证据：locator
   "Cihampelas Walk" 在前导句里，bearer 句只含 {restaurant, serves}
3. **机制**: name-demand face — 问题 "the name of that **restaurant**" ⇒ 提升定义式
   bearer `<ProperName>: this <anaphor≈head>`；anaphor 名词匹配问题中心名词，
   排除 locator-sibling（"Cihampelas Walk: …this shopping center"）与动词冒号项
4. **测试**: TDD 红先 9 miniatures（含 miniature weighted_floor 教训：小池子里
   raw=2 bearer 过不了 floor 10，需 N 填充；语料 N=4480 自动满足）
5. **验证**: census 8 行全枚举恰 1 变化 → 接线 → 9/9 绿 → suite 10352→10361
   (junit 双臂) → live-500 PASS tripwire（恰 1 pred change / 恰 1 drift / 290）

### 教训
- **junitxml 是本机可靠的 suite 计数方式**：exec 后台会话里 pytest stdout 重定向
  到文件会拿到空文件（缓冲/截断问题，junit_base.xml 完整可靠）
- C558 台账 10347 vs 今日实测同 HEAD base 10352：**±5 测量口径漂移**——suite
  计数要在同日同法测 base，否则 delta 对不上
- exec 会话跑长任务（>30min）时 grep 管道全程无输出是缓冲，看 `ps aux` CPU%
  确认在跑，别慌

### 台账
- experiments.tsv 第 669 行（66adc0a）
- 新 authoritative chain: /tmp/c559/live500_c559.json
- next queue: ollama oracle (human-blocked) / run_amg packaging / report judge
  fingerprint / MCP registry publish


## 20:16 — Deep Exploration: Research #032 (Production Agent Memory Architecture)

**Topic:** Mem0 v3 vs Graphiti vs LTM — architecture lessons for amg npm positioning
**Source:** HEARTBEAT.md medium-priority (competitive window tightening)

### Key findings:
1. **ADD-only revolution**: Mem0 v3 dropped UPDATE/DELETE → 3x latency reduction + higher accuracy. Counterintuitive but sound.
2. **Entity resolution is table stakes**: Both Mem0 v3 and Graphiti have it. amg doesn't. Critical gap.
3. **Bi-temporal tracking**: Graphiti's valid_from/valid_to on facts enables point-in-time queries no vector store can do.
4. **Entropy as retrieval signal**: amg's unique advantage — connect entropy toolkit to retrieval scoring for a novel differentiator.
5. **Production gap**: Mem0 scores 92.5 LoCoMo but only 48.6 BEAM-10M. Graph-based retrieval should scale better.

### Code: TemporalFactTracker (8 tests, verified)
- Bi-temporal fact tracking with auto-invalidation
- Entity alias resolution
- Contradiction detection
- Point-in-time queries

### Next actions added to MEMORY.md:
- amg: EntityResolver (~50 lines + ~40 tests)
- amg: bi-temporal edge tracking (~30 lines + ~30 tests)
- amg: entropy_weighted_retrieval() (~40 lines + ~50 tests)
- amg README: "agency-grade graph memory for self-evolving agents" positioning

### Quality check:
✅ Runnable code (8 tests pass)
✅ 5 novel insights (ADD-only, entity resolution, entropy retrieval, LTM positioning, production gap)
✅ Project connection (informs npm publish strategy)
✅ Competitive analysis (6-system, 12-dimension matrix)
✅ Implementation roadmap (5 prioritized actions)

## Code Lab Evening (21:00-21:20)

**4 cycles, all KEEP. Baseline 4951 → Final 5068 (+117 tests).**

### Cycle 292: von_neumann_entropy() + spectral_entropy_profile()
- **First spectral entropy** — Shannon entropy of normalised Laplacian eigenvalues
- K_n maximises: H = ln(n-1). Empty graph = 0. 
- `_laplacian_eigenvalues()` shared infra helper (builds L = D - A, applies `_sym_eigenvalues`)
- `spectral_entropy_profile()` dashboard: algebraic connectivity, spectral gap, spectral radius, participation ratio (complexity), eigenvalue list, entropy ratio
- +50 tests (all pass). Commit `405f82c`.

### Cycle 293: TemporalEntropyTracker
- Phase transition detection via first/second derivatives of spectral entropy
- 5 phases: growth / consolidation / forgetting / transition / insufficient
- Supports 7 entropy indices (von_neumann, sombor, randic, tsallis, renyi, augmented_zagreb, edge_betweenness)
- Report with mean_rate, volatility, trajectory labels
- +37 tests (all pass). Commit `e2c9f53`.

### Cycle 294: quantum_jensen_shannon_distance()
- Spectral inter-graph metric via Laplacian density matrices
- True metric: symmetric, bounded [0, √ln2], satisfies triangle inequality (all verified)
- Captures global topology differences invisible to degree-based methods
- Complementary to `entropy_distance()` (degree-based JSD)
- +26 tests (all pass). Commit `b26c4e7`.

### Cycle 295: entropy_profile() spectral extension
- Extended `entropy_profile()` with `spectral_values` and `spectral_raw_values` fields
- Now includes von_neumann alongside 15 degree/distance/centrality indices
- `spectral_count` field in profile dict
- +4 tests (all pass). Commit `1ebfe1f`.

### Entropy API totals now:
- **17 APIs** across 3 categories:
  - Degree-based (7): sombor, reduced_sombor, randic, zagreb_m1, abc, ga, augmented_zagreb
  - Distance/Edge-partition/Centrality (8): harary, wiener, szeged, gutman, schultz, closeness_vitality, eigenvector_centrality, edge_betweenness
  - Spectral (2): von_neumann, quantum_jensen_shannon_distance
- Plus: entropy_profile (16 indices), spectral_entropy_profile (dashboard), TemporalEntropyTracker (phase detection)
- Generalized: tsallis, renyi
- Inter-graph: entropy_distance (degree-based), quantum_jensen_shannon_distance (spectral)


## C515 key-development-2 (autoresearch) — age_diff keep

- Cron 00:00 循环：C515 age_diff（第 12 个 counting form，self-age 锚定年份算术）kept，commit `1c78ab9` + 台账 `9ad1aec`
- multi_session 57→60/133 = 0.451（+3/-0，serial A/B fresh PRE；PRE 57 与 C514 POST 完全一致，环境零漂移）
- 3 个问题形态：older-than-me（grandma 75th−32=43）/ older-than-when-I（32yo−at-25=7）/ will-I-be-when（32+next year=33）；self-age 四语法 + 事件锚；每锚须唯一值否则落穿
- census 恰好 3 fire（ms133+full500 一致）全 was-wrong 零劫持；suite 10020 绿（+18）
- 坑：question_id[:8] 孪生撞车（ba358f49_abs 覆盖 ba358f49）伪造生产失败——工具层 bug 伪装数据异常家族第 5 例；stash-then-crash 险情（手动 pop 恢复）
- 详见 memory/2026-08-26-key-development-2.md
- ⚠️ 并发协调：memory_graph.py 在 00:19 被会话 e04d222d（22MB 大会话）修改（+24 行未提交）——非 C515 文件，未进本次 commit，按 C508 规则留给 owner；下轮看到 M memory_graph.py 勿误提交/误恢复

## C514 key-development-1 (autoresearch) — museum_count keep

- Cron 23:00 循环：C514 museum_count（第 11 个 counting form）kept，commit `dcd0996` + experiments `e90e037`
- multi_session 55→57/133 = 0.429（+2/-0，serial A/B，fresh PRE @ 6934b6c）
- 机制：question gate（museum/gallery + visit + 可选月份窗）、venue 身份（TitleSeq/引号标题/generic-event 拒绝）、realization 墙（future/speculative 拒绝）、venue 级日期聚合（undated 不虚构窗口）
- **本轮最重要教训**：abs twin 判分走 `meta["abstained"]`，不是数值包含——"0" vs GT "0. You did not mention…" exact 判 False，我最初的 containment 假设被证伪。零 venue = 预设失败 = 诚实 abstain（呼应 C513：silence ≠ 0 claim）。counting 路径 abstained 改为映射 ABSTAIN_ANSWER（验证过仅 museum 返回它，其他 form 不受影响）
- 取证弃案：stated-number（681a1674 假阳性）、weddings（venue-merge 过度 question-specific）、dinner parties（证据薄）
- census 纪律再次有效：恰好 2 fires（full500+ms133 一致）零 hijack
- flaky 记录：test_large_flush_performance 在全量并行时 1 次超时，单独跑过，与 patch 无关
- multi_session 残余 76 wrong；下轮候选家族池已收窄

## 03:00 测试循环（cron project-testing-morning）

- **幂等检查通过**：experiments.tsv 最后一行 08-25 23:20，无晨间循环记录，无并发同任务。
- **发现并修复**：amg `test_large_flush_performance` 全量跑挂（与 prompt-mgr coverage 并发时 CPU 争用），隔离跑过 → wall-clock flake。修 `TestPerformanceEdgeCases` 全部 4 个紧阈值（0.5/2.0/0.1/0.01s → 2/10/1/0.5s），仍能抓 O(n²) 回归。commit `0b3c94d`。
- **验证**：文件 16/16 ×3；全量 10040 passed + 5 subtests / 0 fail，且与 context-forge 并发跑（复现原 flake 条件下通过）。
- **基线**：amg 10040（注：`pytest -x` 在第 3242 个 flake 处截断，曾掩盖全量数）；context-forge 1492/1492；prompt-mgr 324/324 @ 99% cov；agent-task-cli 1599（未重跑，慢）；ams 慢套件未跑。
- **in-flight 保护**：`projects/agent-memory-graph/memory_graph.py` 有另一会话未提交的 search-cache WIP（+24 行），只提交了 test 文件，未动。

## Documentation Morning Cron (04:00)

**幂等三查**: 08-26 无前序 doc-morning 产物（memory 文件均来自 KO/key-development cron），git log 20:00 UTC 后零文档提交 → 首次触发，执行。

**发现**: agent-memory-graph README 漂移 — 徽章 9963 vs 实测 10040（pytest collect），C512-C516 五 cycle 零文档覆盖。弃权语义（abstention）是 08-21 后最大未记录主题：C513 预设有失败门、C514 计数家族首个 ABSTAIN、C515 age_diff、C516 普通名词换轨陷阱、C512 RECORD-NEGATIVE。

**完成** (71618e3):
1. **新建 `TUTORIAL-ABSTENTION.md`** (~3.6KB) — C448→C516 弃权家族教程：
   - 核心洞察：强检索在 _abs 题上制造幻觉（C511 HEAD：18/24 编造、0 弃权）
   - 三律：预设有失败优先于形态门 / 零提及测试要硬 / census 纪律
   - 家族谱系图：entropy(C448) → pairwise(C489) → pref(C498) → neg_exist(C513) → museum(C514) → common-noun(C516)
   - 关键概念：fall-through ≠ ABSTAIN、弃权写进指标（8.6% 是特性）、专名/普通名词检测器为何分开
   - C512 负结果书挡：virtual-flip census 前置关卡（insight #254）
2. **README 修复**: 徽章 9963→10040、对比表、测试节 511/302天→516/303天、教程链接 ×4
3. **README 新增 Cycles 512-516 节**（36 行）：五 cycle 摘要，数字全部对照 commit message + experiments.tsv 核验（abs24 6→8、abs30 10→15、multi 0.414→0.459）

**验证**: 教程引用的 4 个测试文件 76 tests 全绿（0.26s）；badge 与 pytest collect 一致。

## 05:00 essay-generation-morning cron 完成

- 博文《Flake 不是随机性，是答非所问：绊网、IPC 污染与 fail-fast 盲区》已发布，commit 2b9b9d5
- 素材：experiments.tsv 过去 24h 的 0b3c94d（amg 绊网化）+ 8876445（cqc IPC 静默守卫）真实 diff
- 主题与昨 05:03 的 wiring 随笔（44e471c）刻意错开：本次聚焦 flake 三分类
- 幂等三查通过：博客仓近 10 分钟无 commit，无双触发
- 注：Pages CDN 对新 post 文件有 ~2 分钟 404 延迟（index 先出、post 后到），验证需等 + 重试，勿急着回滚

## 08:00 github-trending-deep-analysis cron 完成

- 幂等三查通过：今日 memory 无 trending 记录、sessions_list 无同任务并发 → 首次触发
- 深度分析两项目：**apache/maka**（Apache 孵化 local-first Agent 工作台，Runtime Host 单一执行权威 + 事件日志为唯一事实源 + eval Cell/attempt 模型）和 **DietrichGebert/ponytail**（110k★，七级懒梯规则集 + 教科书级基准自我纠正：#126 批评后重建四臂 agentic benchmark，自曝 SessionStart hook 污染 baseline 的 bug）
- 报告：飞书文档 https://feishu.cn/docx/ZXkMdjKB4oYABAxTnRfcCPW1nFh（create→write 118 块→read 验证 119 块非空 ✓）
- 候选弃选：claude-obsidian（事务化写入协议与今晨 memory_graph.py 并发痛点正中，已放"其他值得关注"节，值得后续单独研究）、openhuman（商业产品，特性巡礼为主）
- 可行动增量（报告 2.5/3.5 节）：earliest-valid-attempt 权威规则进 experiments 台账、七级懒梯并入 AGENTS.md 自查清单、A/B 实验加 caveman 式对照臂

## 05:04 essay-generation-morning cron（双触发·后到会话）

- 先到触发已于 **05:01:51** 提交 `2b9b9d5` 并推送：博文《Flake 不是随机性，是答非所问：绊网、IPC 污染与 fail-fast 盲区》（posts/flake-tripwire-ipc-failfast-2026-08.html + index 引用）
- 本会话 05:04 触发，幂等三查（产物/远端 commit/并发）确认已完成 → **不重做不重发**
- 真实增量：补存 memory/daily-posts/2026-08-26-flake-tripwire-ipc-failfast.md 本地副本（先到触发未留 memory 副本）

## 19:14 GitHub 创新点分析（cron）
- 报告：`memory/github-creative-2026-08-26.md`
- 今日/本周最热：ponytail 111k⭐（反过度工程 skill，-54% LOC 且安全 100%，诚实基准）；OpenViking 33k⭐ 火山引擎 Agent 上下文数据库（viking:// 文件系统 + L0/L1/L2 分层，LoCoMo 80-83%）
- 3 个与本项目直接相关：ai-memory 明确支持 OpenClaw；OpenViking 分层设计可印证 agent-memory-service；ponytail 基准方法论可抄

## 20:08 deep-exploration-evening 重复触发（第 5 例 cron 双触发模式）
- 20:00 主轮已完成 Research #089（笔记 20:06 + MEMORY #089/Next 更新 commit a0625f7 20:07），本轮 20:08 为重复投递
- 按 TOOLS.md 幂等纪律未重做，仅验证质量门：risk_coverage_aurc.py 复跑输出与笔记逐字节一致（AURC 0.3969/0.3942、calib-vs-SP 0.4559/0.1304）✓、MEMORY 150-151 行 #089 + C517 候选 ⓪ 在位 ✓
- 无漏记增量需要补

## 20:10 deep-exploration-evening 双触发（幂等纪律生效例）
- 20:06 前一 run 已产出 Research #089 笔记（abstention/selective prediction/risk-coverage）但未发博客即终止
- 20:10 本 run 按「只补真实增量」：跳过研究，直接执行第二阶段博客发布 → commit 117936f，远端验证通过
- 博文：《越会推理的 AI 越不敢说"我不知道"》posts/abstention-risk-coverage-2026-08.html，中文 2653 字

## 21:00 code-lab-evening — mcp-server-toolkit 四连 cycle（0→33 tests，4 keep）

- **C1 6c62c03**: npm test DOA（jest 在 devDeps 但 node_modules 未装，MODULE_NOT_FOUND）→ node --test 重接线 + 9 测试 hermetic CLI 套件。真 bug：`--type grpc` 静默接受写入垃圾 transport → stdio|sse|stdlib 校验 exit 1（A/B 验证：测试在未修复代码上确实失败）。
- **C2 81a54ee**: validate 命令从 7 行占位变真实现（JSON+YAML、结构规则、重复工具名带首次索引引用、--schema 自定义校验）。+13。
- **C3 ba799cf**: generate 命令——配置→dist/index.ts codegen，validation gate 拒绝坏配置、覆盖保护、sse 诚实不支持。真 bug：generate 默认 mcp-server.yaml 而 init 写 mcp-server.json，默认路径永远找不到。+9。
- **C4 d7e327e**: init 模板的病情遗传——生成项目 test:'jest' 无配置无 ts-jest 无测试文件且 test/ 被 tsconfig 排除 → 子项目 npm test 出生即 DOA（scripts-wiring 家族第 5 例、首例传染后代）。修为 tsc+node --test，冒烟测试用 cwd 读配置（不 import src——顶层 main() 连 stdio 会挂起 runner）。+1。
- 台账时间戳一度写快 70 分钟，已修正（教训：ledger 手写时间前先 date）。
- scripts-wiring bug 家族累计 5 例：amk(655c173) / ai-dev-tools(e542697) / amg(b52e6ba) / mcpt 本体(6c62c03) / mcpt 生成模板(d7e327e)。



## Cron: deep-exploration-evening (20:00) — 深度研究 #009

**主题**: Context Engineering Layer 实现路径 — 从理论到代码

**研究来源**:
- **LCM** (arXiv:2605.04050) — Lossless Context Management，Voltropy PBC。DAG 层级压缩 + 三级升级 + 零成本续行。OOLONG 超越 Claude Code。
- **Searchat** (GitHub开源) — Verbatim + Distilled 双层索引，11x token 缩减。DuckDB + sentence-transformers。
- **Aeon** (arXiv:2601) — 神经符号记忆管理
- **Active Context Compression** (arXiv:2601) — Context Bloat 自主解决

**关键产出**:
1. 完整 TypeScript ContextEngineeringLayer 实现（~300行）
2. 代码已验证可运行（npx tsx 通过，all tests passed）
3. 4 条关键洞察 + 5 步实现路径
4. 与 amg 的具体集成方案

**核心洞察**: LCM 的 GOTO→结构化编程类比是本轮最重要的思想。LLM 擅长生成，不擅长管理自己的状态。确定性代码管状态，LLM 管生成。

**笔记**: catalyst-research/exploration-notes/2026-07-14-context-engineering-layer-lcm-distillation.md

## Cron: documentation-morning (04:00)

完成了 3 个文档改进：

1. **code-lab/README.md** — 从零创建（之前 0 行）。涵盖 10+ 子项目的分类索引表格、设计哲学、快速开始指南
2. **skills/github-trending/README.md** — 从 21 行扩充到完整文档。新增输出示例、CLI 参数表、故障排除、技术细节
3. **skills/x-trends/README.md** — 从 27 行扩充到完整文档。新增国家代码表、CLI 参数表、JSON 输出示例、依赖说明

效果：三个文档从「勉强够用」提升到「新人友好」，覆盖安装→用法→输出→故障排除的完整生命周期。

## Deep Research #008 (20:00)
**Topic: Memory Security & Hybrid Architecture** — Long-context vs external memory trade-off

**Key findings:**
- ShadowMerge achieves 93.8% attack success on Mem0 graph memory — relation-channel conflicts are the #1 threat
- HMARS proves managed memory hierarchy beats flat retrieval even with long contexts
- OSL-MR proves memory retention is NP-hard — validates amg's heuristic approach
- CoreMem shows Fisher-Rao metric > cosine similarity (+4.5pp LoCoMo)
- 10+ memory poisoning papers in May-July 2026 — security is THE frontier

**Strategic insight for amg:** Position as "security-first graph memory" — phantom detection + integrity checker + cascade invalidation differentiate from Mem0.

**Next: Cycle 239 candidate** — RelationIntegrityChecker (~40 tests, runnable demo verified ✅)

**Research note:** catalyst-research/exploration-notes/2026-07-14-memory-security-hybrid-architecture.md

## 21:00 Code Lab Evening — amg Cycles 239-242

**4 cycles, all KEPT, 0 rollbacks. 2983→3128 (+145 tests).**

### Cycle 239: immutable_store + grep() + expand()
- LCM + Searchat inspired. Append-only log auto-populated by add().
- `immutable_retrieve(node_id)`, `immutable_all(limit)`, `immutable_count()`
- `grep(pattern)` — full-history search across ALL records (incl. deleted/compacted)
- `expand(node_id)` — lossless recovery (live → immutable fallback)
- +41 tests

### Cycle 240: compact_node() three-level upgrade
- Level 0 (LLM detailed) / Level 1 (LLM bullet) / Level 2 (deterministic truncate)
- Pluggable `summarizer` callback for LLM levels; auto-fallback to level 2
- `_compact_data()`: dict compaction (truncate strings, replace nested with type+count)
- `compact_batch()` + `compact_stats()`
- Renamed to `compact_node()` to avoid collision with existing `compact()` merge method
- +34 tests

### Cycle 241: serialize() token-budget serialization
- Searchat-inspired pointer format: `[node_id] label (kind) data_preview`
- Weight-ordered greedy packing into char budget (1 token ≈ 4 chars)
- Edge summary within remaining budget
- `serialize_compact()` convenience: auto-compact low-weight → serialize
- Compacted node detection
- +34 tests

### Cycle 242: RelationIntegrityChecker
- ShadowMerge defense (arXiv:2605.09033): 93.8% attack rate on graph memory
- Three checks: value_conflict / confidence_anomaly / origin_mismatch
- `integrity_quarantine()`: auto-quarantine by severity threshold
- `integrity_score` [0,1]
- +36 tests

## 22:25 Evening Dev — amg Cycle 243

**1 cycle, KEPT, 0 rollbacks. 3128→3165 (+37 tests).**

### Cycle 243: semantic_speed_gate + selective_filter
- **semantic_speed_gate()**: RoMem-inspired edge volatility detection. Weighted fraction of edges within time window. Returns speed/stability/velocity/verdict (volatile/active/stable). Uses clock_log for edge timestamps.
- **speed_gate_batch()**: batch speed gate for all/selected nodes, sorted descending.
- **volatile_nodes()**: shortcut for top volatile nodes (prioritize consolidation).
- **selective_filter()**: Context Engineering multi-criteria node pruning. Weight range, kind whitelist/blacklist, quarantine, staleness, freshness. Order-preserving.
- **selective_filter_report()**: summary with input/output counts, drop_rate.
- +37 tests (10 gate + 6 batch + 3 volatile + 13 filter + 5 report)
- 233rd consecutive day, 0 regressions

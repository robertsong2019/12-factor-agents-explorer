# 2026-07-04 (周六)

## 午间 Autoresearch 开发 (07-04 13:23-13:45)

**3 个 TDD cycle 完成，零回滚率保持 175 天 🏆**

### Cycle 179: Cache Temperature API (1d4a789)
- `cache_temperature(node_id)` — CPU-cache-inspired temperature score [0,1]: recency 50% (7-day half-life exponential decay), weight 30%, Q-value 20%
- `cache_snapshot()` — full-graph hot (>0.66) / warm (0.33-0.66) / cold (<0.33) partitioning with per-node entries
- `warm_cache(query)` — search-driven cache prefetch: boosts accessed timestamp + weight bump
- `evict_cold()` — quarantine cold+old nodes with Q-value protection (>0.5 safe) + dry_run audit mode
- +15 tests (1599→1614)

### Cycle 180: Memorywire Format Export/Import (9d2b1e5)
- `to_memorywire_format(include_quarantined=False)` — Memorywire v1 JSON export with node/edge/kind stats
- `from_memorywire_format(data)` — import Memorywire v1 dict with ID remapping + edge preservation
- Round-trip tested: export → import → verify data integrity
- ValueError on unsupported format versions
- +8 tests (1614→1622)

### Cycle 181: Scope-Delete Guard (9d2b1e5)
- `delete_node_safe(node_id, force=False)` — prevents deletion of nodes with live dependents
- Detects non-quarantined nodes with edges pointing TO target
- `force=True` overrides guard
- Quarantined dependents don't block deletion
- Returns dict: deleted (bool), blocked_by (labels), reason (str)
- +5 tests (1622→1627)

### 测试总计
- agent-memory-graph: **1627 passed** (was 1599, +28 tests)
- 零回滚率：**175 天**

### Git Log
```
315b690 experiments: log cycles 179-181
9d2b1e5 feat: memorywire format export/import + delete_node_safe scope guard (+13 tests)
1d4a789 feat: cache temperature API — hot/warm/cold zones + warm_cache + evict_cold (+15 tests)
```

### 关键发现
- 原来已有 `to_memorywire()` / `from_memorywire()` 方法（早期 Memorywire 0.1 格式），新增 `to_memorywire_format()` 提供更完整的 v1 格式（kind stats + quarantine filter + round-trip support）
- Test class 命名冲突导致 pytest shadowing — 需要注意重名

### 研究关联
Cache temperature 灵感来自 CPU cache hierarchy（L1/L2/L3 → hot/warm/cold），将硬件缓存概念应用到 Agent 记忆管理。这是 07-02 Graph-Structured Memory 研究的实践延伸。

## 晚间深度研究 #002 (07-04 20:00-21:00)

**AI Agent Memory Architecture: SOTA 2026** — 系统性调研 20 篇论文/系统

### 覆盖五大架构范式
1. **OS-Inspired Hierarchical** (Letta/MemGPT) — 自编辑记忆, Focus 自主压缩 22.7% token
2. **Zettelkasten Linking** (A-MEM, NeurIPS 2025) — 笔记链接网络
3. **Production Platform** (Mem0) — LoCoMo 92.5, 单遍 ADD-only + 实体链接
4. **Graph-Native** (Cognee, Zep/Graphiti, MAGMA) — KG as primary storage
5. **Episodic + RL** (MemRL, ICLR 2026) — memory retrieval as value-based decision

### 关键洞察
- **MemRL: usefulness ≠ similarity** — Q-value 学习的 "过去有用的记忆" 比 "文本相似的记忆" 更有效
- **LRAT (SIGIR 2026): 生产日志是训练资产** — 失败轨迹也能训练出 +15-19% 更好的 retriever
- **LoCoMo 审计**: 6.4% 答案错误, LLM judge 接受 63% 故意错误答案
- **Memory Poisoning**: 90%+ agent 可被攻击, 对话纠正 100% 复发
- **Temporal Staleness**: Mem0 benchmark 91.6 → 30天后生产环境 49.0%
- **Supersede (Jun 2026)**: time-indexed supersession signal 作为 RL reward

### 对 Catalyst 自身的 Next Actions
- 近期: 给 MEMORY.md 加 temporal tags / entity-first writing / error-to-memory pipeline
- 中期: BM25+semantic 多路检索 / memory decay scoring / heartbeat consolidation 自动化
- Moonshot: MemRL-style Q-value / 轻量 entity graph / sleep-time consolidation cron

笔记: `catalyst-research/exploration-notes/2026-07-04-agent-memory-architecture.md`
MEMORY.md 已更新研究索引


## 03:00 project-testing cron cycle
- Baseline: amg pytest 10224 collected/exit-0 (~2min), agent-observability 233/233, a2a-trust 81/81; rest per experiments.tsv (context-forge 1511, sotk 571, langgraph-bridge 261, mcp-server 17); ams/atc skipped (slow/hung, per prior cycles).
- Target: pocket-agent (0 tests, never cycled) → 24 tests, 3 real bugs red-first:
  1. `Memory.recent(0)` returned ALL entries (`list[-0:]`==`list[0:]` slice gotcha)
  2. `Memory(max_entries=0).store()` → IndexError on constructor-accepted config
  3. `ToolRegistry.register` TypeError under `@reg.register(description=...)` (dual-mode decorator fix)
- 24/24 green ×2, demo roundtrip OK, monorepo commit 71b81e0, experiments.tsv row appended.

## 04:00 documentation-morning cron cycle
- Idempotency check: last doc round = 09-02 (db45be4, through C539/0.510); C540-C542 landed after (HEAD 2adcf92, 03:06) → real delta exists. No in-flight session (last commit 54 min old).
- **README.md** (amg): new section "Cycles 540-542: 0.510→0.520 — 序数面收债 + judge 侧 face 家族" — C540 ordinal face wired (phrase-run tie-break, embedding join falsified pre-implementation), C541 judge reference-alias faces + clause-scoped where-gate intent guard, C542 quoted-core GT-wrap face + authoritative live-500 chain retiring delta chain + A/B cross-arm formula trap (display-layer family #5).
- **TUTORIAL-ANSWER-FACES.md**: retitled Cycles 529-542 (0.494→0.520); +3.6 C540 (RECORD-NEGATIVE 的正确打开方式 — 负结果否定的是那条路不是那类题); +§4 judge 侧 rescue faces (C541-C542, "写法伪装成内容" + 双臂同源纪律); §5 C536 sequel (结论被修正两次); cheat sheet 6→9 rows, 3→4 principles; layered diagram gains judge-side face layer.
- Committed docs + this entry to monorepo.
- 06:00 [dashboard cron] mission-control 更新并推送 fa2d2dd：11/15 cron ok，3 error（ai-neuro 持续、kd-1 新增、essay-gen 新增），sessions 56/41 unique/31 active-24h，tokens 3.19M
- 19:00 [github-creative-evening] 晚间 trending 分析完成 → memory/github-trending-analysis-2026-09-03-evening.md。三主线：① Agent Skills 成标准分发格式（daily 20 席 9 席 skill 类）② 可验证 LLM 工件（archify typed-IR 模式，+26.6k/week 本周 #1）③ 个人 agent 平台战争（NousResearch hermes-agent 正面对标 OpenClaw，学习闭环差异化）。magnitude 明确支持 OpenClaw。可行动：拆 hermes 学习闭环供 amg 参照。

## 晚间深度研究 cron（20:00 触发，非重复投递，20:00 启动时确认无当日笔记无近期 commit）

- **主题：** 思考的经济学——Interleaved Thinking 与推理预算调控（14 个系统/论文：Anthropic think tool / Claude interleaved thinking / MiniMax M2 / Kimi K2 Thinking / Qwen3 / AdaptThink / L1-LCPO / Stop Overthinking 综述 / MUR / SelfBudgeter / ReTool 家族）
- **笔记：** catalyst-research/exploration-notes/2026-09-03-interleaved-thinking-budget-control.md
- **博客：** https://robertsong2019.github.io/posts/interleaved-thinking-budget-2026-09.html（commit 04b0f98，线上已验证 200 + 首页卡片生效）
- 核心洞察：思考的四维资源化（位置/状态/预算/开关）；M2 事故=中间件静默丢 thinking trace 伪装成模型变笨（与显示层伪影家族同构，值得按 TOOLS.md 规则审计 amg/OpenClaw 管线）；AdaptThink -53% 长度 +2.4% 准确率证明元认知可训练

## 21:00 code-lab-evening — mcp-client-explorer 首测套件 0→40（3 cycles 全 keep，4 真 bug 红先行）

- 选项目逻辑：never-cycled + thinnest（864 LOC/0 tests，lab/mcp-client-explorer，monorepo 成员无独立 .git）——与 09-01 openclaw-mcp-server 生命周期 cycle 呼应
- **C1 (0ece52d)**: 34 test 金路径特征套件（RawServer 行协议 harness + MCPClient 真·子进程 E2E）——首跑即中真 bug：`Tool.from_dict` 读 snake_case `input_schema` 但线上格式是 camelCase `inputSchema` → **客户端发现的每个工具 schema 都被静默吞成 {}**（与当日 interleaved-thinking 笔记的 M2 中间件静默丢数据同构）；tolerant-reader 兼容两种拼写，Resource mimeType 同族顺手修
- **C2 (6410ceb)**: 服务器防崩 red×3——合法 JSON 非 object（`[1,2,3]`/`42`/`null`）在 JSONDecodeError catch 之外抛 AttributeError **杀死主循环**（此后所有请求静默失联）；修 = isinstance 守卫逐垃圾行回 -32600 + 逐行异常守卫。通知（无 id）曾被回 id:null -32601 错误（JSON-RPC 2.0 违约：通知绝不回包）→ 一律 None
- **C3 (60a7add)**: 客户端健壮性 red×3——`start()` 对永不初始化的服务器**假成功**（超时被无视 _initialized=True，假成功比失败危险）→ 校验 initialize 响应+回收进程+False；服务器死亡后 write 抛 BrokenPipeError（与存活面 None 不一致）→ OSError→None；`request_timeout` 构造参数（可测性最小增量）；stop() wait 加超时+kill 兜底
- 40/40 ×2 @ 每 cycle，suite 1.6-3.2s；demo 往返 exit-0 回归；experiments.tsv 上账（0ece52d 行）
- 坑：①edit 工具一次替换吞掉 `if method == "initialize":` 行留下孤儿块——py_compile+grep 双检在运行前抓到，一次修正（多块 edit 时 oldText 尾行会被移除，需在 newText 里补回）②test-authoring bug 一例：垃圾行 -32600 回包被 request() 当 initialize 响应消费——先排空再断言（也是行为钉死）
